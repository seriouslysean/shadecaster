---
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Shadecaster - Shadow Lamp STL Generator</title>
    <meta
      name="description"
      content="Browser-based shadow lamp STL generator - convert images to 3D-printable shadow lamps"
    />
    <style>
      :root {
        color-scheme: light;
      }

      *,
      *::before,
      *::after {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Inter", "Segoe UI", system-ui, -apple-system, sans-serif;
        background: #fff;
        color: #000;
        min-height: 100vh;
      }

      .page {
        width: min(960px, 100%);
        margin: 0 auto;
        padding: 2rem 1.5rem 3rem;
        display: flex;
        flex-direction: column;
        gap: 2rem;
      }

      .page__header {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
      }

      .page__title {
        font-size: clamp(2rem, 6vw, 3rem);
        letter-spacing: -0.02em;
      }

      .page__subtitle {
        font-size: 1rem;
        line-height: 1.5;
        max-width: 44ch;
      }

      .card {
        border: 1px solid #000;
        padding: 1.5rem;
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
      }

      .upload {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }

      .upload__label {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        padding: 0.85rem 1.25rem;
        border: 1px solid #000;
        font-weight: 600;
        cursor: pointer;
        background: #fff;
        color: #000;
      }

      .upload__label:hover,
      .upload__label:focus-visible {
        background: #000;
        color: #fff;
      }

      .upload__input {
        position: absolute;
        width: 1px;
        height: 1px;
        padding: 0;
        margin: -1px;
        overflow: hidden;
        clip: rect(0, 0, 0, 0);
        border: 0;
      }

      .upload__meta {
        display: grid;
        gap: 0.5rem;
        font-size: 0.9rem;
      }

      .upload__meta strong {
        font-weight: 600;
      }

      .preview {
        border: 1px solid #000;
        padding: 1rem;
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }

      .preview__canvas {
        width: 100%;
        height: auto;
        display: block;
      }

      .controls {
        display: grid;
        gap: 1rem;
      }

      .controls__group {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
      }

      .controls__label {
        font-size: 0.85rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.08em;
      }

      .controls__input {
        border: 1px solid #000;
        padding: 0.65rem 0.75rem;
        font-size: 1rem;
        width: 100%;
        background: #fff;
        color: #000;
      }

      .controls__range {
        display: flex;
        flex-direction: column;
        gap: 0.35rem;
      }

      .controls__value {
        font-size: 0.85rem;
      }

      .actions {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
      }

      .button {
        border: 1px solid #000;
        padding: 0.85rem 1.25rem;
        font-size: 1rem;
        font-weight: 600;
        background: #fff;
        color: #000;
        cursor: pointer;
      }

      .button:hover:not(:disabled),
      .button:focus-visible:not(:disabled) {
        background: #000;
        color: #fff;
      }

      .button:disabled {
        opacity: 0.4;
        cursor: not-allowed;
      }

      .status {
        font-size: 0.95rem;
        border-left: 2px solid #000;
        padding-left: 0.75rem;
      }

      .footer {
        font-size: 0.85rem;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
      }

      .footer a {
        color: inherit;
      }

      .hidden {
        display: none;
      }

      :focus-visible {
        outline: 2px solid #000;
        outline-offset: 3px;
      }

      @media (min-width: 1024px) {
        .page {
          padding: 3rem 2rem 4rem;
        }

        .controls {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .actions {
          flex-direction: row;
          justify-content: flex-start;
        }
      }
    </style>
  </head>
  <body>
    <main class="page">
      <header class="page__header">
        <p>🕯️ Shadecaster</p>
        <h1 class="page__title">Shadow lamp STL generator</h1>
        <p class="page__subtitle">
          Upload a high-contrast silhouette. We crop to a square automatically and generate a
          tea light-ready shade so the projected shadow matches your image.
        </p>
      </header>

      <section class="card">
        <div class="upload">
          <label for="image-upload" class="upload__label">Upload image</label>
          <input
            type="file"
            id="image-upload"
            class="upload__input"
            accept="image/png,image/jpeg,image/jpg,image/webp"
          />
          <div class="upload__meta">
            <p>
              <strong>Image format:</strong> Black silhouette on white or transparent background.
            </p>
            <p>
              <strong>Aspect ratio:</strong> We center-crop to a square during upload so the
              silhouette stays undistorted.
            </p>
          </div>
        </div>

        <div id="preview-section" class="preview hidden">
          <p><strong>Preview (square crop)</strong></p>
          <canvas id="preview-canvas" class="preview__canvas"></canvas>
        </div>

        <div class="controls">
          <div class="controls__group">
            <label for="dome-diameter" class="controls__label">Dome diameter (mm)</label>
            <input
              type="number"
              id="dome-diameter"
              class="controls__input"
              value="55"
              min="50"
              max="200"
              step="5"
            />
          </div>

          <div class="controls__group">
            <label for="dome-height" class="controls__label">Dome height (mm)</label>
            <input
              type="number"
              id="dome-height"
              class="controls__input"
              value="30"
              min="20"
              max="150"
              step="5"
            />
          </div>

          <div class="controls__group">
            <label for="fin-thickness" class="controls__label">Fin thickness (mm)</label>
            <input
              type="number"
              id="fin-thickness"
              class="controls__input"
              value="0.8"
              min="0.5"
              max="5"
              step="0.1"
            />
          </div>

          <div class="controls__group">
            <label for="base-height" class="controls__label">Base height (mm)</label>
            <input
              type="number"
              id="base-height"
              class="controls__input"
              value="5"
              min="5"
              max="30"
              step="1"
            />
          </div>

          <div class="controls__group controls__range">
            <label for="angular-resolution" class="controls__label">Angular resolution</label>
            <input
              type="range"
              id="angular-resolution"
              class="controls__input"
              value="120"
              min="24"
              max="360"
              step="6"
            />
            <span class="controls__value" id="angular-value">120 fins</span>
          </div>

          <div class="controls__group controls__range">
            <label for="threshold" class="controls__label">Image threshold</label>
            <input
              type="range"
              id="threshold"
              class="controls__input"
              value="128"
              min="0"
              max="255"
              step="1"
            />
            <span class="controls__value" id="threshold-value">128</span>
          </div>
        </div>

        <div class="actions">
          <button id="process-btn" class="button" disabled>Process image</button>
          <button id="download-btn" class="button hidden">Download STL</button>
        </div>

        <p id="info-text" class="status" aria-live="polite">
          Upload an image to get started.
        </p>
      </section>

      <footer class="footer">
        <p>
          Print with dark filament for maximum contrast. All processing happens in your browser.
        </p>
        <p>
          <a href="https://github.com/seriouslysean/shadecaster" target="_blank" rel="noreferrer">
            View on GitHub
          </a>
        </p>
      </footer>
    </main>

    <script>
      import '../main';
    </script>
  </body>
</html>
